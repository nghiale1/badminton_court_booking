import 'package:flutter/material.dart';

class IconNotify extends StatelessWidget {
  const IconNotify({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(Icons.notifications),
      onPressed: () {},
    );
  }
}